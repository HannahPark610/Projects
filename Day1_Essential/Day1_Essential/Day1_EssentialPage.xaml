<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:local="clr-namespace:Day1_Essential" 
    x:Class="Day1_Essential.Day1_EssentialPage">
 <StackLayout
        Orientation="Vertical"
        BindingContext="{x:Reference slider1}">
        <Label Text="{Binding Value,
                              StringFormat='Font Size : 16'}"
               Opacity ="{Binding Value}"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               x:Name="label1"
               FontSize="16" />
        <Label Text="{Binding Value,
                              StringFormat='You cant blame gravity for falling in love'}"
               Opacity ="{Binding Value}"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               x:Name="label1"
               FontSize="16" />
        
        <Slider HorizontalOptions="FillAndExpand"
                x:Name="slider1"/>
    </StackLayout>
</ContentPage>